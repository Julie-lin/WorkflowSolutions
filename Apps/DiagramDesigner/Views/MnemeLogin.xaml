<UserControl x:Class="DiagramDesigner.Views.MnemeLogin"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Width="600" Height="341">
    <Grid HorizontalAlignment="Left" Width="567" Margin="0,0,-81,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="200"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Border Grid.Column="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Image VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Source="/DiagramDesigner;component/Resources/Images/PasteBig.png">
                
            </Image>
        </Border>
        <Border Grid.Column="1" Grid.Row="1">
            
        </Border>

    </Grid>
</UserControl>
